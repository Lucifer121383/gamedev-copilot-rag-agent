# IncidentCopilot 初学者学习指南

这份指南的目标不是让你背代码，而是让你能回答三个问题：系统为什么这样设计、一次请求到底经历了什么、如果线上结果不对应该从哪里查。

建议按顺序学习。每个模块都先读代码，再运行例子，最后用自己的话完成验收题。

## 第一模块：先理解项目解决什么问题

普通聊天机器人依靠模型已有知识回答，可能不知道公司的新版本、内部错误码和历史 Bug。RAG 会先从指定资料里检索证据，再把问题和证据交给回答模块。

本项目比基础 RAG 多做了三件事：

1. 识别这是崩溃、服务故障、性能问题还是普通咨询。
2. 根据情况调用历史问题、版本变化、测试用例和工单工具。
3. 把来源、分数、工具结果和执行轨迹都显示出来，方便排查错误。

请在网页分别点击游戏研发和企业软件示例，观察两个空间返回的来源不会混在一起。

验收题：

- 为什么企业订单问题不能检索到游戏 Bug？
- 为什么系统不能在用户没有要求时自动创建工单？
- 为什么 P1 诊断仍然建议人工复核？

## 第二模块：文档如何进入知识库

重点文件是 `app/document_loader.py` 和 `app/chunker.py`。

完整数据流：

```text
原始文件 -> 识别格式 -> 提取章节或记录 -> 清洗空白 -> 重叠切分 -> Chunk对象
```

文档不能直接整篇检索。文本过长时，一个问题只和其中一小段有关，整篇向量或关键词会稀释相关信息。因此系统使用 `chunk_size` 控制每块长度，使用 `chunk_overlap` 在相邻块之间保留上下文。

每个 Chunk 不只有正文，还保留：

- `domain`：game 或 enterprise
- `source`：来源文件
- `doc_type`：日志、版本记录、历史 Bug 等
- `page` 和 `section`：页码与章节
- `metadata`：错误码、平台、模块、版本等结构化信息

动手练习：

1. 打开 `data/game/design_docs/equipment_system.md`。
2. 在网页上传一份自己写的 Markdown 文档。
3. 点击重建索引。
4. 用文档里的专属词提问，检查来源是否命中。

验收题：如果 `overlap` 等于或大于 `chunk_size`，循环为什么可能无法正常前进？

## 第三模块：混合检索为什么比单一路径稳定

重点文件是 `app/retriever.py`。

项目默认组合四类信号：

1. BM25：适合错误描述、函数名和日志关键词。
2. 稠密向量：默认使用可离线运行的 Hashing 向量，也可切换 BGE Embedding。
3. 精确匹配：版本、平台、模块和 `E-EQP-500` 这类错误码获得额外权重。
4. RRF 与重排：先融合各路排名，再用轻量规则或 CrossEncoder 调整候选顺序。

最终分数不是模型的绝对正确率，而是用于排序和证据门控的校准相关度。网页展开来源后能看到 BM25、Dense、Exact、Fusion 和 Rerank 分数，便于判断某条资料为什么排在前面。

动手练习：

```powershell
python scripts\evaluate.py
```

然后删除问题里的错误码再运行一次单独查询，比较日志和历史 Bug 的排名变化。

验收题：为什么错误码完全一致时，日志应该比只出现相同版本号的设计文档更靠前？

## 第四模块：Agent 不等于无限自主

重点文件是 `app/workflow.py`、`app/planner.py`、`app/agent.py` 和 `app/service.py`。

本项目的 Agent 是受控工作流。它会按顺序执行：

```text
接收问题 -> 结合最近对话改写检索词 -> 混合检索 -> 证据门控 -> 诊断
-> 规划工具 -> 执行只读工具 -> 按需人工审批 -> 写工具 -> 组织回答 -> 保存结果
```

`workflow.py` 使用 LangGraph 明确声明节点和路由，并用 SQLite Checkpoint 保存中断状态。`service.py` 负责资源初始化、并发锁和后台索引任务。`agent.py` 是大模型不可用时的确定性诊断路径，`planner.py` 负责模型 Function Calling 和安全降级。

这里把稳定的业务规则写在代码里，而不是全部藏进提示词，原因是代码更容易测试、审查和约束。

动手练习：在网页先问完整问题，再追问“那这个怎么处理”，观察系统如何使用最近的用户消息补充检索词。

验收题：为什么会话历史只取最近几条，而不是把所有对话无限追加？

## 第五模块：工具调用与安全边界

重点文件是 `app/tools.py` 和 `app/database.py`。

工具不是普通 Python 函数列表。每个工具都有标准名称、说明、参数类型、必填字段和 `additionalProperties: false`。执行前还会进行白名单和字段校验。

四个工具中，前三个是只读操作：

- `search_bug_history`
- `get_version_changes`
- `generate_test_cases`

`create_bug_ticket` 是写操作，因此只有用户明确说创建或登记时才会加入计划；加入计划后也不会立刻执行，LangGraph 会通过 `interrupt` 暂停，等待用户批准或拒绝。

幂等的含义是同一个业务请求重复执行，效果仍然只发生一次。系统根据会话和消息生成稳定键，数据库对该键设置唯一约束。网络重试时会返回原工单，而不是创建第二张。

动手练习：连续两次发送完全相同的创建工单请求，确认工单号相同，并观察第二次显示“幂等复用”。

验收题：为什么仅靠前端把按钮禁用，不能真正防止重复工单？

## 第六模块：生成、引用与失败降级

重点文件是 `app/generator.py`。

没有 API Key 时，系统使用结构化诊断，把检索和规则的结果按固定格式展示。配置模型接口后，回答模块会将用户问题、初步诊断、工具结果和编号来源一起提交给模型，并要求关键结论附来源。

调用可能遇到超时、网络错误、限流和服务端错误。系统进行有限次数重试，仍失败就回到结构化回答，不让整个诊断接口崩溃。

要记住：降级不是假装模型成功，而是明确使用能力较弱但可用的备用路径。

验收题：为什么 400 类参数错误通常不应该反复重试，而 429 或 500 可以短暂重试？

## 第七模块：API、测试和上线

重点文件是 `app/main.py`、`tests/`、`Dockerfile` 和 `render.yaml`。

FastAPI 负责输入校验、路由和响应。耗时的同步任务通过线程池执行，避免直接阻塞异步事件循环。`/docs` 会自动展示接口文档。

运行测试：

```powershell
python -m pytest -q
```

测试的价值不是证明系统永远没问题，而是固定已经确认的行为。例如：

- game 查询不能返回 enterprise 文档
- 非法 `top_k` 必须报错
- 没有明确要求时不能创建工单
- 重复请求必须复用同一工单
- API 能返回诊断、来源和轨迹

Render 免费实例使用临时文件系统，所以网页可以展示完整流程，但上传文件、会话和工单不适合当永久数据。生产系统需要数据库、对象存储、权限和审计。

## 完整验收清单

当你能独立完成下面内容，就可以在面试中把项目写成“熟悉”或“能够完成”，而不仅是“了解”：

- 用三分钟说清楚 RAG、Agent 和 Function Calling 的区别
- 画出从文档到答案的数据流
- 解释 BM25、Dense、Exact、RRF 和 Rerank 的关系
- 解释工作空间隔离和最小分数过滤
- 解释为什么写工具同时需要明确意图、人工审批和幂等键
- 解释 LangGraph Checkpoint 如何恢复暂停的工作流
- 修改一个错误码的排序规则，并让测试通过
- 新增一个只读工具并写测试
- 看懂一次 Agent trace，定位问题发生在哪一步
- 说出当前 SQLite、临时磁盘和小型评测集的局限
- 在不看答案的情况下演示一个游戏场景和一个企业场景
