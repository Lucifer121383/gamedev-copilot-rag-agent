# GameDev Copilot 面试讲解指南

## 一分钟项目介绍

我做的是一个面向研发测试和故障排查的 RAG Agent。它有游戏研发和企业软件两个隔离的知识空间，可以导入设计文档、版本记录、日志、历史缺陷和测试用例。检索层不是只做关键词搜索，而是组合中文字符 TF-IDF、英文词项和错误码、平台、版本等元数据分数。Agent 会先分类故障并判断严重度，再调用历史缺陷、版本变化和用例生成工具。创建工单属于写操作，只有用户明确要求才会执行，并通过幂等键避免重复工单。网页可以看到每条证据、分数、工具结果和完整执行轨迹，模型接口失败时也能降级为结构化诊断。

## 三分钟展开顺序

1. 业务问题：研发资料分散，普通问答无法稳定处理内部错误码、版本差异和工单动作。
2. 数据处理：多格式解析，按章节和结构化记录切分，保留平台、模块、版本等元数据。
3. 检索策略：中文字符、英文词项、精确错误码和可选语义向量混合排序。
4. Agent 编排：检索、诊断、规划、工具执行、生成、持久化，每一步都有 trace。
5. 工程边界：双空间隔离、参数白名单、明确授权、幂等、最大步数、失败重试与降级。
6. 评测：8 条演示集 Hit Rate@3 为 100%，同时强调样本很小，只用于回归测试。
7. 生产改进：PostgreSQL、对象存储、鉴权、审计、向量数据库、真实标注集和监控。

## 面试官常问问题

### 为什么不直接把所有文档发给大模型

上下文长度有限，成本和延迟会增加，大量无关信息也会干扰回答。先检索相关片段可以缩小范围，并保留可检查的来源。

### 为什么使用混合检索

纯语义检索擅长同义表达，但错误码、版本号和函数名需要精确匹配。纯关键词又不擅长自然语言改写。混合检索让两类信号互补，并能展示分数组成。

### 为什么默认不用向量数据库

演示数据量小，Scikit-learn 足以完成可重复的本地检索，也降低运行和部署门槛。数据量增大或需要在线增量索引时，可以迁移到 Elasticsearch、pgvector、Milvus 等服务。

### Agent 和普通 RAG 的区别

普通 RAG 主要是检索后回答。本项目还会根据问题规划和调用工具，例如查询版本、生成测试用例和创建工单，并记录工具结果与执行轨迹。

### 如何减少幻觉

只允许依据召回资料和工具结果下结论；证据不足时降低置信度；关键结论要求来源；P1 和低置信度转人工；模型失败时降级，避免返回未经验证的自由生成内容。

### 如何防止重复创建工单

为会话和消息生成稳定幂等键，数据库对该键加唯一约束。重复请求先读取已有记录，返回同一个工单号。仅在前端禁用按钮不够，因为网络重试和并发请求仍可能绕过前端。

### 当前项目最大的不足是什么

演示数据和评测集较小，分类规则偏确定性，SQLite 不适合多实例，公开部署使用临时磁盘，也还没有真实用户权限和线上监控。下一步会优先增加真实标注集、无答案评测、PostgreSQL、对象存储和鉴权审计。

### 如果检索结果不相关，你怎么排查

先确认工作空间和过滤条件，再看文档是否解析成功、分块是否破坏关键信息、错误码和元数据是否被保留，然后比较字符、词项、语义和元数据各分量，最后通过离线用例调整权重，而不是只凭单个例子改参数。

### 为什么要有最大执行步数

工具失败或规划错误可能导致重复循环。最大步数是成本和安全保护，达到上限时停止自动执行并转人工，而不是无限消耗资源。

## 现场演示脚本

1. 切换到游戏研发空间，询问 `E-EQP-500`。
2. 展开来源，指出日志、历史 Bug 和版本记录的不同分数。
3. 展开工具结果与 Agent trace，说明每一步的职责。
4. 再发“请创建Bug工单”，展示显式授权和工单结果。
5. 重复相同请求，展示幂等复用。
6. 切换到企业软件空间，询问 `DB-POOL-503`，证明数据隔离与通用能力。

## 简历写法

可以写：

> 设计并实现研发故障诊断 RAG Agent，支持游戏与企业软件双知识空间、多格式文档解析、混合检索、带引用诊断和 Function Calling 工具编排；为写操作加入显式授权、参数校验与幂等控制，并通过 FastAPI、SQLite、Pytest、Docker 完成接口化、持久化、测试和部署。

在你还不能独立解释代码前，建议写“完成项目并持续学习”，不要写“精通生产级 Agent 平台”。能讲清边界和取舍，通常比堆更多技术名词更有说服力。
