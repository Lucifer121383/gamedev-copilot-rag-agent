# IncidentCopilot 面试讲解指南

## 一分钟项目介绍

我做的是一个面向研发故障排查的 RAG Agent，包含游戏研发和企业软件两个隔离知识空间。检索层组合 BM25、BGE 或离线 Hashing 向量、错误码精确匹配、RRF 融合和重排，并在生成前用证据门控决定回答还是拒答。工作流使用 LangGraph 编排检索、诊断、Function Calling 和工具执行；创建工单会动态中断，只有人工批准后才恢复写入，同时用唯一幂等键防止重复工单。前端可查看来源分数、工具结果、Agent 轨迹和延迟，模型接口不可用时会安全降级。

## 三分钟展开顺序

1. 业务问题：研发资料分散，普通问答无法稳定处理内部错误码、版本差异和工单动作。
2. 数据处理：多格式解析，按章节和结构化记录切分，保留平台、模块、版本等元数据。
3. 检索策略：BM25、向量、精确错误码、RRF 和 Reranker 多阶段排序。
4. 证据门控：未知错误码或低相关结果拒答并转人工，而不是强行生成。
5. Agent 编排：LangGraph 节点、条件路由、Checkpoint、动态审批和恢复。
6. 工程边界：双空间隔离、工具参数白名单、人工审批、幂等、步数限制、重试和降级。
7. 评测：50 条构造问题覆盖同义改写、无答案、Prompt Injection 和工具越权，默认模式 Hit Rate@3 为 97.78%。
8. 生产改进：共享 Checkpoint、对象存储、RBAC、集中限流、真实标注集和可观测平台。

## 面试官常问问题

### 为什么不直接把所有文档发给大模型

上下文长度有限，成本和延迟会增加，大量无关信息也会干扰回答。先检索相关片段可以缩小范围，并保留可检查的来源。

### 为什么使用混合检索

纯语义检索擅长同义表达，但错误码、版本号和函数名需要精确匹配。纯关键词又不擅长自然语言改写。混合检索让两类信号互补，并能展示分数组成。

### 为什么默认不用向量数据库

演示数据量小，FAISS 平面索引足以完成可重复的本地检索，也降低运行和部署门槛。数据量增大、需要多租户过滤或在线增量索引时，可以迁移到 pgvector、Milvus、Qdrant 或 Elasticsearch 等服务。

### Agent 和普通 RAG 的区别

普通 RAG 主要是检索后回答。本项目还会根据问题规划和调用工具，例如查询版本、生成测试用例和创建工单，并记录工具结果与执行轨迹。

### 如何减少幻觉

只允许依据召回资料和工具结果下结论；证据不足时降低置信度；关键结论要求来源；P1 和低置信度转人工；模型失败时降级，避免返回未经验证的自由生成内容。

### 如何防止重复创建工单

第一层要求用户明确表达创建意图；第二层通过 LangGraph `interrupt` 暂停并等待人工审批；第三层为会话和消息生成稳定幂等键，数据库对该键加唯一约束。重复请求返回同一个工单号。仅在前端禁用按钮不够，因为网络重试和并发请求仍可能绕过前端。

### 当前项目最大的不足是什么

演示数据和评测集仍然较小；默认 Hashing 向量的语义泛化有限；进程内限流和本地 Checkpoint 不适合多实例；上传链路也没有 OCR、病毒扫描和对象存储。下一步会优先引入真实标注集、共享 Checkpoint、RBAC、集中限流和线上评测监控。

### 如果检索结果不相关，你怎么排查

先确认工作空间和过滤条件，再看文档是否解析成功、分块是否破坏关键信息、错误码和元数据是否被保留，然后比较字符、词项、语义和元数据各分量，最后通过离线用例调整权重，而不是只凭单个例子改参数。

### 为什么要有最大执行步数

工具失败或规划错误可能导致重复循环。最大步数是成本和安全保护，达到上限时停止自动执行并转人工，而不是无限消耗资源。

## 现场演示脚本

1. 切换到游戏研发空间，询问 `E-EQP-500`。
2. 展开来源，指出日志、历史 Bug 和版本记录的不同分数。
3. 展开工具结果与 Agent trace，说明每一步的职责。
4. 再发“请创建Bug工单”，展示工作流暂停和人工审批。
5. 批准后展示写入结果，再重复同一请求展示幂等复用。
6. 切换到企业软件空间，询问 `DB-POOL-503`，证明数据隔离与通用能力。

## 简历写法

可以写：

> 设计并实现研发故障诊断 RAG Agent，构建 BM25、BGE/Hashing 向量、FAISS、RRF 与 Reranker 混合检索链路，并基于 LangGraph 完成证据门控、Function Calling、可恢复人工审批和幂等工单写入；使用 FastAPI、React、SQLAlchemy、PostgreSQL/SQLite、Pytest、Docker 与 CI 完成接口化、可观测和部署。

在你还不能独立解释代码前，建议写“完成项目并持续学习”，不要写“精通生产级 Agent 平台”。能讲清边界和取舍，通常比堆更多技术名词更有说服力。
