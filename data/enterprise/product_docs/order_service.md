# 企业订单服务技术说明

版本范围：2.3至2.4
模块：订单服务、数据库、支付服务
平台：Linux、Web

订单API通过连接池访问orders数据库。每个请求结束前必须释放数据库连接。批量导出和订单详情聚合查询不得在事务中执行外部支付调用。

错误码 DB-POOL-503 表示连接池在3秒内无法取得可用连接。常见原因包括慢SQL、事务未提交、异常路径未释放连接和短时间流量突增。出现该错误时应先执行只读健康检查，再分析慢查询和当前版本改动。

